# Access code shown only in the paid section of your note article.
# Baked into the built JS bundle at build time — anyone who inspects the
# bundle's source can read it, so treat it as a soft gate, not a secret.
# Rotate it (change this value, then rebuild + redeploy) if it leaks.
VITE_ACCESS_CODE=
